{% extends '::base.html.twig' %}

{% block title %}
Edition
{{ parent() }}
{% endblock %}

{% block stylesheets %}
{{ form_stylesheet(edit_form) }}
{% endblock %}

{% block titre %}Edition {% endblock %}

{% block body %}


    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                Edition d'une salle
            </h1>
            <!-- <div class="alert alert-info">
            <h3>Saisissez le nom des salles qui seront utilisées tout au long de l'année scolaire.</h3><br>
            <h4>La liste des salles sera proposée lors de la création d'un évenement</h4>
        </div> -->
    </div>
</div>
<div class="row">
    <div class="col-md-2">
    </div>
    <div class="col-md-8">
            <div>
            {{ form_start(edit_form) }}
            {{ form_errors(edit_form) }}
                <div class="form-group">
                    <label for="start">
                        {{ form_label(edit_form.startDate) }}
                    </label>
                    {{ form_errors(edit_form.startDate) }}
                    {{ form_widget(edit_form.startDate, { 'attr': { 'class' : 'form-control' } } ) }}
                </div>
                <div class="form-group">
                    <label for="end">
                        {{ form_label(edit_form.endDate) }}
                    </label>
                    {{ form_errors(edit_form.endDate) }}
                    {{ form_widget(edit_form.endDate, { 'attr': { 'class' : 'form-control' } } ) }}
                </div>
                <div class="form-group">
                    <label for="calendar">
                        {{ form_label(edit_form.calendar) }}
                    </label>
                    {{ form_errors(edit_form.calendar) }}
                    {{ form_widget(edit_form.calendar, { 'attr': { 'class' : 'form-control' } } ) }}
                </div>
                <div class="form-group">
                    <label for="subject">
                        {{ form_label(edit_form.subject) }}
                    </label>
                    {{ form_errors(edit_form.subject) }}
                    {{ form_widget(edit_form.subject, { 'attr': { 'class' : 'form-control' } } ) }}
                </div>
                <div class="form-group">
                    <label for="classroom">
                        {{ form_label(edit_form.classroom) }}
                    </label>
                    {{ form_errors(edit_form.classroom) }}
                    {{ form_widget(edit_form.classroom, { 'attr': { 'class' : 'form-control' } } ) }}
                </div>
                <div class="form-group">
                    <label for="notice">
                        {{ form_label(edit_form.notice) }}
                    </label>
                    {{ form_errors(edit_form.notice) }}
                    {{ form_widget(edit_form.notice, {'attr': { 'class' : 'form-control' } } ) }}
                </div>
                <div class="form-group">
                    {{ form_row(edit_form.save, {'label': 'button.update', 'attr': { 'class' : 'btn btn-success' } } ) }}
                    {{ form_end(edit_form) }}
                </div>

                <a class="btn btn-danger" data-toggle="modal" data-target="#modal-delete">Supprimer</a>
                {% include 'AppBundle::modal-delete.html.twig' with { 'form': delete_form } %}
            </div>
    </div>
    <div class="col-md-2">
    </div>
</div>

{% block javascripts %}
  {{ form_javascript(edit_form) }}
<script>

$(function () {
     $('#app_subject_startDate').datetimepicker({

    });
     $('#app_subject_endDate').datetimepicker({
     });
     $("#app_subject_startDate").on("dp.change", function (e) {
         $('#app_subject_endDate').data("DateTimePicker").minDate(e.date);
     });
     $("#app_subject_endDate").on("dp.change", function (e) {
         $('#app_subject_startDate').data("DateTimePicker").maxDate(e.date);
     });
 });
</script>
{% endblock %}
{% endblock %}
