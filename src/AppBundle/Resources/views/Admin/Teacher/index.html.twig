{% extends 'base.html.twig' %}
{% block title %}
Gestion des enseignants(es)/intervenants(es)
{{ parent() }}
{% endblock %}
{% block titre %}Gestion des enseignants(es)/intervenants(es){% endblock %}
{% block body %}

{% for flash_message in app.session.flashbag.get('notice') %}
<div class="flash-notice">
    <div class="alert alert-info">
        <h3>2) a) Ici se trouve la liste des enseignant(e)s et intervenant(e)s.</h3><br>
        <h4>Pour créer une nouvelle entrée, cliquez sur le bouton <em>Ajouter</em></h4><br>
        <h4>Une fois vos entrées créées, rendez-vous dans la rubrique <em>Matières</em> accessible via le menu de gauche</h4>
    </div>
</div>

{% endfor %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                Gestion des enseignants/intervenants
            </h1>
        </div>

    </div>
    <div class="col-md-4">
    </div>
    <div class="col-md-4">
        <div class="table-responsive">
            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th>Nom - prénom</th>
                        <th>Editer</th>
                    </tr>
                </thead>
                <tbody>
                    {% for teacher in teachers %}
                    <tr>
                        <td>{{teacher.lastname}} - {{teacher.firstname}}</td>
                        <td>lien</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
    <div class="col-md-4">
            <a class="btn btn-success" href="{{path('admin_teacher_new')}}">Ajouter</a>
    </div>


{% endblock %}
